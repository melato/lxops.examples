#lxops-v1
devices:
  bin:
    path: /usr/local/bin
    filesystem: host
  etc:
    path: /etc/opt
    filesystem: host
  home:
    path: /home
    filesystem: host
  opt:
    path: /opt
    filesystem: host
  var:
    path: /var/opt
    filesystem: host
include:
- host.yaml
- log.yaml
