#lxops-v1
profile-pattern: (instance).lxops
device-owner: 1000000:1000000
filesystems:
  host:
    pattern: (fshost)/(instance)
    destroy: true
devices:
  etc:
    path: /etc/opt
    filesystem: host
  home:
    path: /home
    filesystem: host
