#lxdops
os:
  name: alpine
description: nginx template
origin: (base-origin)
device-template: (base-template)
include:
- ../../device/standard.yaml
cloud-config-files:  
- ../base/ssh.cfg
- ../nginx/packages.cfg
- ../base/motd.cfg
stop: true  
snapshot: copy  
